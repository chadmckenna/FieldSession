<% title "Roles" %>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Users Count</th>
  </tr>
  <% for role in @roles %>
    <tr>
      <td><%=h role.name %></td>
      <td><%=h role.description %></td>
      <td><%=h role.users_count %></td>
      <td><%= link_to "Show", admin_role_path(role) %></td>
      <td><%= link_to "Edit", edit_admin_role_path(role) %></td>
      <td><%= link_to "Destroy", admin_role_path(path), :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Role", new_admin_role_path %></p>
