- title "New Child"

%h2 Add children to your household

-if current_user.household.num_children > (Child.find(:all, :conditions => {:household_id => current_user.household_id}).count)
  #profile_notice
    %h3 Step 4 of 5
    = "When you created your household, you said that you have #{pluralize(current_user.household.num_children, 'child')}.  This is information that caregivers will be able to see when (and only when) they take care of your children."

= render :partial => 'form'

%p= link_to "View all your children", members_children_path

